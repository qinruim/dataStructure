### 解题思路

智慧的题目

### 代码

* cpp

```cpp
class Solution {
public:
    string replaceSpace(string s) {
        string mt;
        for(int i=0;i<s.size();i++){
            if(s[i]==' '){
                mt.push_back('%');
                mt.push_back('2');
                mt.push_back('0');
                continue;
            }
            mt.push_back(s[i]);
        }
        return mt;
    }
};
```

